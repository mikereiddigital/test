name: Test


on:
  schedule:
    - cron: '50 9 * * *'

env:
  CRON:  '50 9 * * *'

jobs:

  stop-start-instances:
 
    runs-on: ubuntu-latest

    steps:
  
      - name: Test
        if: ${{ github.event.schedule }} == $CRON
        run: |
          echo "It works"