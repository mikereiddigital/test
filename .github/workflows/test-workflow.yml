name: Test


on:
  schedule:
    - cron: '20 12 * * *'

jobs:

  job-2:
 
    runs-on: ubuntu-latest

    steps:
  
      - name: Test
        if: ${{ github.event.schedule }} == '20 12 * * *'
        run: |
          echo ${{ github.event.schedule }}
          echo $(date +%x_%r)
        