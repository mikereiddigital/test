name: Test


on:
  schedule:
    - cron: '*/5 * * * *'

jobs:

  job-1:
 
    runs-on: ubuntu-latest

    steps:
  
      - name: Test
        if: github.event.schedule == '*/5 * * * *'
        run: echo "It works"
      - name: Every time
        run: echo "This step will always run"