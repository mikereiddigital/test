name: Test


on:
  schedule:
    - cron: '15 13 * * *'

jobs:

  job-2:
 
    runs-on: ubuntu-latest

    steps:
  
      - name: init
        run: echo $(date +%x_%r)

      - name: Test
        if: github.event.schedule == '15 13 * * *'
        run: |
          echo ${{ github.event.schedule }}