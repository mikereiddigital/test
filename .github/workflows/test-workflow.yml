name: Test


on:
  schedule:
    - cron: '55 9 * * *'

env:
  CRON:  '55 9 * * *'

jobs:

  stop-start-instances:
 
    runs-on: ubuntu-latest

    steps:
  
      - name: Test
        if: ${{ github.event.schedule }} == $CRON
        run: |
          echo "It works"