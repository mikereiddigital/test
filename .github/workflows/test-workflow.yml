name: Test


on:
  schedule:
    - cron: '*/15 * * * *'

env:
  CRON:  '*/15 * * * *'

jobs:

  stop-start-instances:
 
    runs-on: ubuntu-latest

    steps:
  
      - name: Test
        if: ${{ github.event.schedule }} == $CRON
        run: |
          echo "It works"