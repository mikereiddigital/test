name: Test


on:
  schedule:
    - cron: '55 9 * * *'

jobs:

  job-1:
 
    runs-on: ubuntu-latest

    steps:
  
      - name: Test
        if: github.event.schedule == '55 9 * * *'
        run: echo "It works"
      - name: Every time
        run: echo "This step will always run"