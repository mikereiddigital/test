name: Test


on:
  schedule:
    - cron: '09 15 * * *'

jobs:

  job-2:
 
    runs-on: ubuntu-latest

    steps:
  
      - name: init
        if: github.event.schedule == '09 15 * * *'
        run: |
          echo $(date +%x_%r)
          echo ${{ github.event.schedule }}